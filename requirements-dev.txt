# Tooling for local development and CI. Pinned so a new release of ruff or pytest can't break the build on its own.
#
# Check these are still current before you commit them the first time:
#   pip index versions <dependency>

docker==7.2.0
Flask==3.1.3
Jinja2==3.1.6
prometheus-client==0.26.0
psycopg2-binary==2.9.12
pytest==9.1.1
pyyaml==6.0.3
reportlab==5.0.1
ruff==0.16.1
yamllint==1.38.0
