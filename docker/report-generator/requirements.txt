# Runtime dependencies for the report-generator service container.
# Pinned so a rebuild months from now can't silently pull a breaking release.
#
# Check these are still current before you commit them the first time:
#   pip index versions <dependency>

Jinja2==3.1.6
psycopg2-binary==2.9.12
pyyaml==6.0.3
reportlab==5.0.0
